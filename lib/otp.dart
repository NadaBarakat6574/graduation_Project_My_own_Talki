import 'package:flutter/material.dart';

class Otp extends StatelessWidget {
  static const String routename = 'routename';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('sass',style: TextStyle(color: Colors.red),)
        ],
      ),
    );
  }
}
